# LLM 层 实现计划（v1 第四阶段）

> **For agentic workers:** REQUIRED SUB-SKILL: 用 @superpowers:executing-plans 逐个任务执行本计划。每个任务内部遵循 @superpowers:test-driven-development：先写失败的测试，跑一遍确认它失败，再写最小实现，跑绿，提交。步骤用 `- [x]` 复选框跟踪。
>
> 涉及 Claude / Anthropic API 的任务开工前先读 @claude-api skill——模型 ID、`output_config` 的形状、异常类名都以它为准，不要凭记忆写。

**Goal:** 给闭环装上 spec §4.2 与 §7 的两处 LLM 能力——`ari plan` 在用户填完预测后给出 AI 的**定性**判断（排序 / 方向 / 混淆因素，不给数值），`ari review` 在复盘时基于本项目历史提出**有针对性**的追问。全程满足 spec §8：LLM 不可用时一切照常离线工作。

**Architecture:** 三层。最底下 `llm/` 是两个 provider 适配器加一个统一入口，唯一碰网络的地方；中间 `advising.py` / `recall.py` / `probing.py` 是纯函数——构造 prompt、校验响应、检索历史；最上面 `cli.py` 只做接线与降级。每个适配器拆成「一个薄的 `_request`（碰 SDK）+ 一堆纯函数（解析与校验）」，测试只喂 fixture 给纯函数，CI 永不打真实 API。

**Tech Stack:** Python ≥3.11、`tomllib`（内置）、官方 `anthropic` SDK、官方 `openai` SDK、typer、rich、PyYAML、pytest。新增两个依赖。

**Spec:** `specs/2026-08-23-experiment-loop-design.md` §4.2、§6、§7、§8、§9
**前置:** 计划一、二、三（均已完成）

---

## 交付定义

```bash
ari plan   -p ~/exp/lr-sweep   # 填完预测并锁定后，并排看到 AI 的那份定性判断
ari review -p ~/exp/lr-sweep   # 复盘草稿顶部带一条基于本项目历史的具体追问
```

拔掉网线（或不配 API key）时，上面两条命令**行为完全不变，只是少了 AI 那一段**，不报错、不阻断、不需要加任何 flag。

## 文件结构

```
src/ari/
  config.py              新增。读 config.toml，解析 [roles] 的 "provider:model"
  llm/__init__.py        新增。complete() 分发 + LLMUnavailable + 纯校验
  llm/claude.py          新增。Anthropic 适配器（import anthropic）
  llm/gpt.py             新增。OpenAI 适配器（import openai）
  advising.py            新增。spec §4.2：AI 的定性预测，prompt 与校验
  recall.py              新增。历史检索：相似的 SURPRISE 与 reflection（纯函数，无 LLM）
  probing.py             新增。spec §7：复盘追问的 prompt 与校验
  cli.py                 改。plan / review 接线，全部走降级路径
tests/
  test_config.py  test_llm.py  test_advising.py  test_recall.py  test_probing.py
  test_cli_plan.py  test_cli_review.py  改
  test_e2e_llm.py        新增。离线降级的端到端
  fixtures/llm/          两家 provider 的响应样本
pyproject.toml           改。加 anthropic、openai
README.md                改。LLM 层的配置与离线保证
```

## 文件命名说明

适配器叫 `claude.py` / `gpt.py` 而不是 `anthropic.py` / `openai.py`。Python 3 默认绝对导入，`src/ari/llm/anthropic.py` 里写 `import anthropic` 其实能拿到顶层包、不会自引用——但读代码的人得先想一遍才敢确定。换个名字，这个问题根本不存在。

## 五条贯穿全局的约束

1. **锚定效应必须靠结构杜绝，不能靠自觉。** spec §4.2：先看到 AI 的预测，用户自己的预测就失去独立性，而独立性正是这个机制的全部价值。所以 LLM 调用**放在预测事件已经写入之后**才发起——不是「先算好、晚点再显示」，而是那时候根本还没算。代价是用户填完要等几秒，值得。
2. **AI 不给数值。** 通用模型对私有数据集上的绝对数值没有有效先验，编出来的精确数字第一次离谱就把信任耗光。schema 里**一个数值字段都没有**，这是结构保证而不是 prompt 里的一句请求。
3. **LLM 不可用是正常状态，不是错误。** spec §8。没装 SDK、没配 key、连不上、被限流、超时——统统收敛成 `LLMUnavailable`，调用方打一行提示继续走。任何一条路径都不许让 `plan` / `review` 退出码非零。
4. **CI 永不打真实 API。** spec §9。碰 SDK 的代码收窄到每个适配器一个 `_request` 函数，测试不碰它；解析与校验是纯函数，用 fixture 喂。断言「结构合法 + 引用的 run key 在本批次真实存在」，不断言模型说了什么。
5. **AI 的输出是原材料，不是判定。** 它只进 `note` 事件存档，绝不参与 verdict。判定内核必须保持纯离线可复现。

> **跑测试：** `uv run pytest`。若本机 `uv` 起不来（沙箱下 uv 缓存目录不可写），等价命令是 `PYTHONPATH=src .venv/bin/python -m pytest`。

---

## Task 1: `config.py` — 配置与角色解析

**Files:**
- Create: `src/ari/config.py`、`tests/test_config.py`

`config.toml` 已经由 `ari init` 生成（`cli.py` 的 `CONFIG_TEMPLATE`），本任务只负责读它。密钥永远只以**环境变量名**的形式出现在配置里，值从环境读——配置文件是要进 git 的。

- [x] **Step 1: 写失败的测试** `tests/test_config.py`

```python
"""config.toml 的读取与角色解析。见 spec §6、§8。

配置缺失、角色没配、环境变量没设——三种情况都必须是「LLM 不可用」这一种
结果，而不是三种不同的崩法。
"""

from __future__ import annotations

import pytest

from ari.config import ModelRef, load_config, resolve_role
from ari.llm import LLMUnavailable

CONFIG = """\
[providers.anthropic]
base_url = "https://api.anthropic.com"
api_key_env = "ANTHROPIC_API_KEY"

[providers.openai]
base_url = "https://api.openai.com/v1"
api_key_env = "OPENAI_API_KEY"

[roles]
reason = "anthropic:claude-opus-5"
"""


def _write(tmp_path, text=CONFIG):
    (tmp_path / "config.toml").write_text(text, encoding="utf-8")
    return tmp_path


def test_missing_config_is_unavailable_not_a_crash(tmp_path):
    with pytest.raises(LLMUnavailable):
        load_config(tmp_path)


def test_malformed_config_is_unavailable(tmp_path):
    with pytest.raises(LLMUnavailable):
        load_config(_write(tmp_path, "这不是 toml [[["))


def test_resolve_role_returns_provider_model_and_key(tmp_path, monkeypatch):
    monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-test")
    config = load_config(_write(tmp_path))

    ref = resolve_role(config, "reason")

    assert ref == ModelRef(
        provider="anthropic",
        model="claude-opus-5",
        base_url="https://api.anthropic.com",
        api_key="sk-test",
    )


def test_unconfigured_role_is_unavailable(tmp_path, monkeypatch):
    monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-test")
    config = load_config(_write(tmp_path))

    with pytest.raises(LLMUnavailable) as exc:
        resolve_role(config, "extract")

    assert "extract" in str(exc.value)


def test_unset_api_key_env_names_the_variable(tmp_path, monkeypatch):
    monkeypatch.delenv("ANTHROPIC_API_KEY", raising=False)
    config = load_config(_write(tmp_path))

    with pytest.raises(LLMUnavailable) as exc:
        resolve_role(config, "reason")

    assert "ANTHROPIC_API_KEY" in str(exc.value)  # 说清楚该设哪个变量


def test_placeholder_model_is_unavailable(tmp_path, monkeypatch):
    # 模板里发下去的是 <strong-model>，没改就用等于没配
    monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-test")
    config = load_config(
        _write(tmp_path, CONFIG.replace("claude-opus-5", "<strong-model>"))
    )

    with pytest.raises(LLMUnavailable):
        resolve_role(config, "reason")


def test_unknown_provider_is_unavailable(tmp_path, monkeypatch):
    config = load_config(
        _write(tmp_path, CONFIG.replace("anthropic:claude-opus-5", "grok:whatever"))
    )

    with pytest.raises(LLMUnavailable) as exc:
        resolve_role(config, "reason")

    assert "grok" in str(exc.value)


def test_role_without_a_colon_is_unavailable(tmp_path):
    config = load_config(_write(tmp_path, CONFIG.replace("anthropic:claude-opus-5", "claude")))

    with pytest.raises(LLMUnavailable):
        resolve_role(config, "reason")
```

- [x] **Step 2: 跑测试确认失败** — `uv run pytest tests/test_config.py -v`，预期 `ModuleNotFoundError: No module named 'ari.config'`

- [x] **Step 3: 实现** `src/ari/config.py`

```python
"""config.toml 的读取与角色解析。见 spec §6。

配置文件会进 git，所以里面只有平台地址与**环境变量名**，密钥的值永远
从环境读。

所有「配不全」的情况——文件不存在、TOML 坏了、角色没配、环境变量没设、
provider 不认识——都收敛成同一个 LLMUnavailable。调用方只需要处理一种
失败，spec §8 要求的离线降级才可能写得干净。
"""

from __future__ import annotations

import os
import tomllib
from dataclasses import dataclass
from pathlib import Path

from .llm import LLMUnavailable

KNOWN_PROVIDERS = ("anthropic", "openai")


@dataclass(frozen=True)
class ModelRef:
    provider: str
    model: str
    base_url: str | None
    api_key: str


def load_config(project_dir) -> dict:
    path = Path(project_dir) / "config.toml"
    if not path.exists():
        raise LLMUnavailable(f"没有 {path}，跳过 AI 那一段")
    try:
        return tomllib.loads(path.read_text(encoding="utf-8"))
    except (tomllib.TOMLDecodeError, OSError) as exc:
        raise LLMUnavailable(f"{path} 读不了：{exc}") from exc


def resolve_role(config: dict, role: str) -> ModelRef:
    """把 [roles] 里的 "provider:model" 解析成一次调用需要的全部信息。"""
    spec = (config.get("roles") or {}).get(role)
    if not isinstance(spec, str) or ":" not in spec:
        raise LLMUnavailable(
            f"config.toml 的 [roles] 里没有配 {role}（形如 {role} = \"anthropic:claude-opus-5\"）"
        )

    provider, _, model = spec.partition(":")
    provider, model = provider.strip(), model.strip()

    if provider not in KNOWN_PROVIDERS:
        raise LLMUnavailable(
            f"不认识的 provider {provider!r}，目前支持 {'、'.join(KNOWN_PROVIDERS)}"
        )
    if not model or model.startswith("<"):
        raise LLMUnavailable(f"[roles] 的 {role} 还是模板占位符，填一个真实的模型名")

    settings = (config.get("providers") or {}).get(provider) or {}
    key_env = settings.get("api_key_env") or ""
    api_key = os.environ.get(key_env) if key_env else None
    if not api_key:
        raise LLMUnavailable(f"环境变量 {key_env or '(未声明 api_key_env)'} 没有设置")

    return ModelRef(
        provider=provider,
        model=model,
        base_url=settings.get("base_url") or None,
        api_key=api_key,
    )
```

- [x] **Step 4: 跑测试确认通过**（本任务依赖 Task 2 的 `LLMUnavailable`；若先做本任务，在 `llm/__init__.py` 里先只放那一个异常类）

- [x] **Step 5: 提交** — `git commit -m "feat: config.toml 读取与角色解析"`

---

## Task 2: `llm/` — 统一入口、异常与纯校验

**Files:**
- Create: `src/ari/llm/__init__.py`、`tests/test_llm.py`
- Modify: `pyproject.toml`

- [x] **Step 1: 写失败的测试** `tests/test_llm.py`

```python
"""LLM 统一入口的纯逻辑。见 spec §8、§9。

这个文件里没有一行会碰网络。适配器的 _request 是唯一碰 SDK 的地方，
测试不调它；能测的部分——分发、异常收敛、响应校验——全是纯函数。
"""

from __future__ import annotations

import pytest

from ari.llm import LLMUnavailable, parse_json_payload

SCHEMA = {
    "type": "object",
    "properties": {"ranking": {"type": "array", "items": {"type": "string"}}},
    "required": ["ranking"],
    "additionalProperties": False,
}


def test_parses_a_well_formed_payload():
    assert parse_json_payload('{"ranking": ["a", "b"]}', SCHEMA) == {"ranking": ["a", "b"]}


def test_payload_wrapped_in_a_code_fence_still_parses():
    # 结构化输出理论上不会带围栏，但模型偶尔会——为这一种情况崩掉不值得
    text = '```json\n{"ranking": ["a"]}\n```'

    assert parse_json_payload(text, SCHEMA) == {"ranking": ["a"]}


def test_non_json_is_unavailable_not_a_crash():
    with pytest.raises(LLMUnavailable):
        parse_json_payload("模型今天不想输出 JSON", SCHEMA)


def test_missing_required_field_is_rejected():
    with pytest.raises(LLMUnavailable) as exc:
        parse_json_payload('{"other": 1}', SCHEMA)

    assert "ranking" in str(exc.value)


def test_wrong_type_is_rejected():
    with pytest.raises(LLMUnavailable):
        parse_json_payload('{"ranking": "不是数组"}', SCHEMA)


def test_extra_field_is_rejected_when_schema_forbids_it():
    with pytest.raises(LLMUnavailable):
        parse_json_payload('{"ranking": [], "extra": 1}', SCHEMA)


def test_top_level_non_object_is_rejected():
    with pytest.raises(LLMUnavailable):
        parse_json_payload('["a"]', SCHEMA)
```

- [x] **Step 2: 跑测试确认失败**

- [x] **Step 3: 实现** `src/ari/llm/__init__.py`

`_validate` 是一个**够用就好**的 JSON Schema 子集校验器：只认 `type` / `properties` / `required` / `items` / `additionalProperties`——这正是我们自己写的那几个 schema 用到的全部关键字。为此引入 `jsonschema` 不划算，跟 `editor.py` 拒绝为一个函数引入 click 是同一笔账。

```python
"""LLM 调用的唯一入口。见 spec §6、§8、§9。

整个项目里只有 llm/claude.py 与 llm/gpt.py 的 _request 会碰网络，其余
全是纯函数——这与 editor.py 只有一处碰 $EDITOR 是同一个结构。

所有失败都收敛成 LLMUnavailable：没装 SDK、没配 key、连不上、被限流、
返回的不是合法 JSON……对调用方而言这些没有区别，都是「这次没有 AI 那
一段」。spec §8 要求非 LLM 功能必须照常工作，只有把失败收敛成一种，
降级代码才写得干净。
"""

from __future__ import annotations

import json
import re


class LLMUnavailable(RuntimeError):
    """这次拿不到 AI 的判断。调用方应当降级，不应当中断。"""


_FENCE_RE = re.compile(r"^\s*```(?:json)?\s*(?P<body>.*?)\s*```\s*$", re.DOTALL)


def parse_json_payload(text: str, schema: dict) -> dict:
    """把模型返回的文本解析成校验过的 dict。"""
    match = _FENCE_RE.match(text or "")
    if match:
        text = match.group("body")
    try:
        data = json.loads(text)
    except (json.JSONDecodeError, TypeError) as exc:
        raise LLMUnavailable(f"模型没有返回合法 JSON：{exc}") from exc

    errors = _validate(data, schema, path="")
    if errors:
        raise LLMUnavailable("模型的输出不符合约定结构：" + "；".join(errors))
    return data


_TYPES = {
    "object": dict,
    "array": list,
    "string": str,
    "boolean": bool,
    "number": (int, float),
    "integer": int,
}


def _validate(value, schema: dict, path: str) -> list[str]:
    """JSON Schema 的一个够用子集：type / properties / required /
    items / additionalProperties。我们自己写的 schema 只用到这些。"""
    where = path or "顶层"
    expected = schema.get("type")
    if expected:
        py = _TYPES[expected]
        # bool 是 int 的子类，别让 True 混过 number 检查
        if isinstance(value, bool) != (expected == "boolean"):
            return [f"{where} 应该是 {expected}"]
        if not isinstance(value, py):
            return [f"{where} 应该是 {expected}"]

    errors: list[str] = []
    if expected == "object":
        for name in schema.get("required", []):
            if name not in value:
                errors.append(f"{where} 缺少字段 {name}")
        properties = schema.get("properties", {})
        if schema.get("additionalProperties") is False:
            for name in value:
                if name not in properties:
                    errors.append(f"{where} 多了字段 {name}")
        for name, sub in properties.items():
            if name in value:
                errors += _validate(value[name], sub, f"{path}.{name}" if path else name)

    if expected == "array" and "items" in schema:
        for index, item in enumerate(value):
            errors += _validate(item, schema["items"], f"{path}[{index}]")

    return errors
```

`pyproject.toml` 的 `dependencies` 加两行。版本下限按两家的当前主版本钉死，不能放宽——`output_config`（Anthropic）与 `response_format` 的 `json_schema` 形状都只在这些版本上存在，放宽会让 `uv sync` 装出一个跑不通的组合：

```toml
    "anthropic>=1.0",
    "openai>=3.3",
```

- [x] **Step 4: 跑测试确认通过；`uv sync` 装上新依赖**

- [x] **Step 5: 提交** — `git commit -m "feat: LLM 统一入口与响应校验"`

---

## Task 3: 两个 provider 适配器

**Files:**
- Create: `src/ari/llm/claude.py`、`src/ari/llm/gpt.py`、`tests/fixtures/llm/*.json`
- Modify: `src/ari/llm/__init__.py`、`tests/test_llm.py`

两家的接口形状（均已从官方源核实，勿凭记忆改）：

| | Anthropic | OpenAI |
|---|---|---|
| 调用 | `client.messages.create(...)` | `client.chat.completions.create(...)` |
| 结构化输出 | `output_config={"format": {"type": "json_schema", "schema": {...}}}` | `response_format={"type": "json_schema", "json_schema": {"name":…, "strict": True, "schema": {...}}}` |
| 取文本 | `next(b.text for b in resp.content if b.type == "text")` | `resp.choices[0].message.content` |
| 系统提示 | 顶层 `system=` | `messages[0]` 的 `role="developer"` |
| 异常 | `anthropic.APIConnectionError` / `RateLimitError` / `APIStatusError` | 同名，在 `openai` 下 |

两个决定与理由：

- **OpenAI 侧用 `chat.completions` 而不是更新的 Responses API。** `config.toml` 明确暴露了 `base_url`，科研环境里指向 vLLM 之类的 OpenAI 兼容服务是常态，而这些服务几乎都实现 `/chat/completions`、极少实现 `/responses`。兼容性在这里比跟进新接口重要。
- **Anthropic 侧带 `thinking={"type": "adaptive"}`，模型默认 `claude-opus-5`。** 排序与混淆因素属于要推理的判断。代价是这要求 Claude 4.6 及以上，配置里换成更老的模型会报错——在 config.toml 的注释里写明。

- [x] **Step 1: 造 fixture 与写失败的测试**

`tests/fixtures/llm/anthropic_advice.json`（Anthropic 响应里 `content[0].text` 的内容）与 `tests/fixtures/llm/openai_advice.json`（`choices[0].message.content` 的内容）各存一份**模型输出的文本**，内容是一份合法的 advice JSON。

> 这两份 fixture 是照官方文档的响应形状手写的，不是真实录制的。有了 key 之后应当用一次真实调用的输出覆盖它们（见 Task 8 的收尾项）。这里如实标注，不假装是录制的。

追加到 `tests/test_llm.py`：

```python
import json
from pathlib import Path

from ari.llm import complete
from ari.llm.claude import extract_text as claude_text
from ari.llm.gpt import extract_text as gpt_text

FIXTURES = Path(__file__).parent / "fixtures" / "llm"


class _Block:
    def __init__(self, text): self.type, self.text = "text", text


class _AnthropicResponse:
    def __init__(self, text): self.content = [_Block(text)]


class _Choice:
    def __init__(self, text): self.message = type("M", (), {"content": text})()


class _OpenAIResponse:
    def __init__(self, text): self.choices = [_Choice(text)]


def test_claude_extracts_the_text_block():
    payload = (FIXTURES / "anthropic_advice.json").read_text(encoding="utf-8")

    assert claude_text(_AnthropicResponse(payload)) == payload


def test_claude_response_without_text_block_is_unavailable():
    empty = type("R", (), {"content": []})()

    with pytest.raises(LLMUnavailable):
        claude_text(empty)


def test_gpt_extracts_the_message_content():
    payload = (FIXTURES / "openai_advice.json").read_text(encoding="utf-8")

    assert gpt_text(_OpenAIResponse(payload)) == payload


def test_gpt_response_with_null_content_is_unavailable():
    with pytest.raises(LLMUnavailable):
        gpt_text(_OpenAIResponse(None))


def test_both_fixtures_survive_the_full_parse(tmp_path):
    for name in ("anthropic_advice.json", "openai_advice.json"):
        data = json.loads((FIXTURES / name).read_text(encoding="utf-8"))
        assert set(data) == {"ranking", "directions", "confounders"}


def test_complete_dispatches_on_provider(monkeypatch):
    from ari.config import ModelRef
    from ari.llm import claude

    seen = {}

    def fake_request(ref, system, user, schema):
        seen["ref"] = ref
        return '{"ranking": ["a"]}'

    monkeypatch.setattr(claude, "request", fake_request)
    ref = ModelRef("anthropic", "claude-opus-5", None, "sk-test")

    assert complete(ref, "s", "u", SCHEMA) == {"ranking": ["a"]}
    assert seen["ref"] is ref


def test_complete_on_an_unknown_provider_is_unavailable():
    from ari.config import ModelRef

    with pytest.raises(LLMUnavailable):
        complete(ModelRef("grok", "x", None, "k"), "s", "u", SCHEMA)
```

- [x] **Step 2: 跑测试确认失败**

- [x] **Step 3: 实现**

`src/ari/llm/claude.py`：

```python
"""Anthropic 适配器。接口形状见 @claude-api skill，勿凭记忆改。

request() 是这个文件里唯一碰网络的函数，测试不调它；extract_text 是
纯函数，用 fixture 测。
"""

from __future__ import annotations

from . import LLMUnavailable

MAX_TOKENS = 16000


def request(ref, system: str, user: str, schema: dict) -> str:
    """发一次请求，返回模型输出的原始文本。"""
    # 延迟导入：ari board 之类的命令不该为一个用不到的 SDK 付启动开销
    try:
        import anthropic
    except ImportError as exc:
        raise LLMUnavailable("没有安装 anthropic SDK（uv sync）") from exc

    client = anthropic.Anthropic(api_key=ref.api_key, base_url=ref.base_url)
    try:
        response = client.messages.create(
            model=ref.model,
            max_tokens=MAX_TOKENS,
            thinking={"type": "adaptive"},
            system=system,
            messages=[{"role": "user", "content": user}],
            output_config={"format": {"type": "json_schema", "schema": schema}},
        )
    except anthropic.APIConnectionError as exc:
        raise LLMUnavailable(f"连不上 {ref.provider}：{exc}") from exc
    except anthropic.AuthenticationError as exc:
        raise LLMUnavailable(f"{ref.provider} 认证失败，检查 API key") from exc
    except anthropic.RateLimitError as exc:
        raise LLMUnavailable(f"{ref.provider} 限流，稍后再试") from exc
    except anthropic.APIStatusError as exc:
        raise LLMUnavailable(f"{ref.provider} 返回 {exc.status_code}：{exc.message}") from exc

    return extract_text(response)


def extract_text(response) -> str:
    for block in getattr(response, "content", []) or []:
        if getattr(block, "type", None) == "text":
            return block.text
    raise LLMUnavailable("Anthropic 的响应里没有文本块")
```

`src/ari/llm/gpt.py`：

```python
"""OpenAI 适配器。

用 chat.completions 而不是更新的 Responses API：config.toml 暴露了
base_url，科研环境里常指向 OpenAI 兼容的自建服务，那些服务几乎都实现
/chat/completions、极少实现 /responses。
"""

from __future__ import annotations

from . import LLMUnavailable


def request(ref, system: str, user: str, schema: dict) -> str:
    try:
        import openai
    except ImportError as exc:
        raise LLMUnavailable("没有安装 openai SDK（uv sync）") from exc

    client = openai.OpenAI(api_key=ref.api_key, base_url=ref.base_url)
    try:
        response = client.chat.completions.create(
            model=ref.model,
            messages=[
                {"role": "developer", "content": system},
                {"role": "user", "content": user},
            ],
            response_format={
                "type": "json_schema",
                "json_schema": {"name": "ari_output", "strict": True, "schema": schema},
            },
        )
    except openai.APIConnectionError as exc:
        raise LLMUnavailable(f"连不上 {ref.provider}：{exc}") from exc
    except openai.AuthenticationError as exc:
        raise LLMUnavailable(f"{ref.provider} 认证失败，检查 API key") from exc
    except openai.RateLimitError as exc:
        raise LLMUnavailable(f"{ref.provider} 限流，稍后再试") from exc
    except openai.APIStatusError as exc:
        raise LLMUnavailable(f"{ref.provider} 返回 {exc.status_code}") from exc

    return extract_text(response)


def extract_text(response) -> str:
    choices = getattr(response, "choices", None) or []
    content = choices[0].message.content if choices else None
    if not content:
        raise LLMUnavailable("OpenAI 的响应里没有内容")
    return content
```

`src/ari/llm/__init__.py` 末尾加分发（在函数内导入，避免子模块与包的循环导入）：

```python
def complete(ref, system: str, user: str, schema: dict) -> dict:
    """按 provider 分发，返回校验过的 dict。失败一律 LLMUnavailable。"""
    from . import claude, gpt

    adapters = {"anthropic": claude, "openai": gpt}
    adapter = adapters.get(ref.provider)
    if adapter is None:
        raise LLMUnavailable(f"不认识的 provider {ref.provider!r}")
    return parse_json_payload(adapter.request(ref, system, user, schema), schema)
```

- [x] **Step 4: 跑测试确认通过**

- [x] **Step 5: 提交** — `git commit -m "feat: Anthropic 与 OpenAI 两个适配器"`

---

## Task 4: `advising.py` — AI 的那份定性预测

**Files:**
- Create: `src/ari/advising.py`、`tests/test_advising.py`

spec §4.2：输出限定三项，**不含数值点估计**。schema 里一个数值字段都没有——这是结构保证。另加一条校验：`ranking` 里的 run 必须都是本批次真实存在的 run，防止模型编出不存在的配置。

- [x] **Step 1: 写失败的测试** `tests/test_advising.py`

```python
"""AI 的定性预测。见 spec §4.2。

断言的是「结构合法 + 引用的 run 真实存在」，不断言模型说了什么——
spec §9 的要求。
"""

from __future__ import annotations

import pytest

from ari.advising import ADVICE_SCHEMA, build_prompt, check_advice
from ari.llm import LLMUnavailable

RUNS = ["model=base", "model=large"]
GOOD = {
    "ranking": ["model=large", "model=base"],
    "directions": [{"variable": "model", "effect": "容量更大通常带来更高准确率，但收益递减"}],
    "confounders": ["两组的数据增强是否一致", "batch size 是否随模型一起变了"],
}


def test_schema_has_no_numeric_field():
    # 结构保证，不是 prompt 里的一句请求
    def types_in(node):
        if isinstance(node, dict):
            if "type" in node and isinstance(node["type"], str):
                yield node["type"]
            for value in node.values():
                yield from types_in(value)

    assert "number" not in set(types_in(ADVICE_SCHEMA))
    assert "integer" not in set(types_in(ADVICE_SCHEMA))


def test_prompt_carries_the_hypothesis_and_every_run():
    prompt = build_prompt("large 比 base 好", {"model": ["base", "large"]}, RUNS, ["top1_acc"])

    assert "large 比 base 好" in prompt
    for run in RUNS:
        assert run in prompt
    assert "top1_acc" in prompt


def test_prompt_forbids_numbers_explicitly():
    prompt = build_prompt("h", {"model": ["base"]}, ["model=base"], ["top1_acc"])

    assert "数值" in prompt  # schema 之外再说一遍，双保险


def test_good_advice_passes():
    assert check_advice(GOOD, RUNS) == GOOD


def test_ranking_naming_a_nonexistent_run_is_rejected():
    bad = {**GOOD, "ranking": ["model=huge", "model=base"]}

    with pytest.raises(LLMUnavailable) as exc:
        check_advice(bad, RUNS)

    assert "model=huge" in str(exc.value)


def test_ranking_missing_a_run_is_rejected():
    with pytest.raises(LLMUnavailable):
        check_advice({**GOOD, "ranking": ["model=large"]}, RUNS)


def test_duplicate_run_in_ranking_is_rejected():
    with pytest.raises(LLMUnavailable):
        check_advice({**GOOD, "ranking": ["model=large", "model=large"]}, RUNS)
```

- [x] **Step 2: 跑测试确认失败**

- [x] **Step 3: 实现** `src/ari/advising.py`

```python
"""AI 的那份判断。见 spec §4.2。

只给三样东西：相对排序、定性方向、混淆因素。**不给数值。**

理由：通用模型对「你的私有数据集上 large 比 base 高几个点」没有任何
有效先验，给出的数字看似精确实则编造；第一次出现明显离谱的数值就会把
信任耗光，而这个功能的全部价值都建立在信任上。相对排序与混淆因素提示
则是模型真正有优势的地方。

schema 里一个数值字段都没有——这是结构保证，不是 prompt 里的一句请求。
"""

from __future__ import annotations

from .llm import LLMUnavailable

ADVICE_SCHEMA = {
    "type": "object",
    "properties": {
        "ranking": {"type": "array", "items": {"type": "string"}},
        "directions": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {"variable": {"type": "string"}, "effect": {"type": "string"}},
                "required": ["variable", "effect"],
                "additionalProperties": False,
            },
        },
        "confounders": {"type": "array", "items": {"type": "string"}},
    },
    "required": ["ranking", "directions", "confounders"],
    "additionalProperties": False,
}

SYSTEM = """\
你在协助一位科研人员做实验前的预判。你只提供三样东西：

1. ranking —— 这些配置按目标指标从好到差的**相对排序**；
2. directions —— 每个变量的影响方向与强弱，用定性语言；
3. confounders —— 对方可能没考虑到的混淆因素。

**绝不给任何数值点估计或区间。** 你对这个私有数据集没有有效先验，编出
来的数字看似精确实则是猜的。ranking 只能使用给定的 run 标识，一个不漏、
一个不多，不要发明新的配置。
"""


def build_prompt(hypothesis: str, dimensions: dict, runs: list[str], metrics: list[str]) -> str:
    lines = [
        f"假设：{hypothesis}",
        "",
        "变量维度：",
        *(f"  {name}: {', '.join(str(v) for v in values)}" for name, values in dimensions.items()),
        "",
        f"关心的指标：{', '.join(metrics)}",
        "",
        "这一批的 run（ranking 必须正好用这些标识）：",
        *(f"  {run}" for run in runs),
        "",
        "只给相对排序、定性方向与混淆因素，不要给任何数值。",
    ]
    return "\n".join(lines)


def check_advice(advice: dict, runs: list[str]) -> dict:
    """结构之外的语义校验：ranking 必须恰好是本批次的 run 的一个排列。"""
    ranking = advice.get("ranking") or []
    unknown = [r for r in ranking if r not in runs]
    if unknown:
        raise LLMUnavailable(f"AI 的排序里出现了不存在的 run：{'、'.join(unknown)}")
    if len(set(ranking)) != len(ranking):
        raise LLMUnavailable("AI 的排序里有重复的 run")
    missing = [r for r in runs if r not in ranking]
    if missing:
        raise LLMUnavailable(f"AI 的排序漏掉了 run：{'、'.join(missing)}")
    return advice
```

- [x] **Step 4: 跑测试确认通过**
- [x] **Step 5: 提交** — `git commit -m "feat: AI 的定性预测，schema 层面杜绝数值"`

---

## Task 5: `ari plan` 接线——填完才展示

**Files:**
- Modify: `src/ari/cli.py`、`tests/test_cli_plan.py`

调用点放在**预测事件已经追加之后**。不是「先算好、晚点再显示」——那样一个手滑就泄漏了；是那时候根本还没发起调用。锚定效应靠结构杜绝。

- [x] **Step 1: 写失败的测试**，追加到 `tests/test_cli_plan.py`

```python
def test_plan_shows_ai_advice_after_predictions_are_written(tmp_path, monkeypatch):
    project = _project(tmp_path)  # 沿用该文件已有的辅助
    monkeypatch.setattr(cli, "edit_text", FakeEditor(DESIGN, PREDICTIONS))
    monkeypatch.setattr(cli, "advise", lambda *a, **k: {
        "ranking": ["model=large", "model=base"],
        "directions": [{"variable": "model", "effect": "容量更大通常更好"}],
        "confounders": ["增强是否一致"],
    })

    result = runner.invoke(cli.app, ["plan", "-p", str(project)])

    assert result.exit_code == 0
    assert "增强是否一致" in result.output


def test_plan_asks_the_model_only_after_the_events_are_on_disk(tmp_path, monkeypatch):
    """锚定效应：调用发起时，预测必须已经落盘。"""
    project = _project(tmp_path)
    monkeypatch.setattr(cli, "edit_text", FakeEditor(DESIGN, PREDICTIONS))
    seen = {}

    def spy(*args, **kwargs):
        events, _ = read_events(project / "runs.jsonl")
        seen["predictions_on_disk"] = len([e for e in events if e.type == "prediction"])
        raise LLMUnavailable("测试里不打真实 API")

    monkeypatch.setattr(cli, "advise", spy)
    runner.invoke(cli.app, ["plan", "-p", str(project)])

    assert seen["predictions_on_disk"] == 2


def test_plan_without_llm_still_succeeds(tmp_path, monkeypatch):
    project = _project(tmp_path)
    monkeypatch.setattr(cli, "edit_text", FakeEditor(DESIGN, PREDICTIONS))

    def unavailable(*args, **kwargs):
        raise LLMUnavailable("没配 key")

    monkeypatch.setattr(cli, "advise", unavailable)
    result = runner.invoke(cli.app, ["plan", "-p", str(project)])

    assert result.exit_code == 0
    events, _ = read_events(project / "runs.jsonl")
    assert len([e for e in events if e.type == "prediction"]) == 2


def test_ai_advice_is_archived_as_a_note(tmp_path, monkeypatch):
    project = _project(tmp_path)
    monkeypatch.setattr(cli, "edit_text", FakeEditor(DESIGN, PREDICTIONS))
    monkeypatch.setattr(cli, "advise", lambda *a, **k: {
        "ranking": ["model=large", "model=base"], "directions": [], "confounders": ["x"],
    })

    runner.invoke(cli.app, ["plan", "-p", str(project)])

    events, _ = read_events(project / "runs.jsonl")
    notes = [e for e in events if e.type == "note"]
    assert len(notes) == 1
    assert notes[0].payload["kind"] == "ai_advice"
    assert notes[0].payload["advice"]["confounders"] == ["x"]
```

- [x] **Step 2: 跑测试确认失败**

- [x] **Step 3: 实现**

在 `cli.py` 加一个 `advise` 函数（测试 monkeypatch 的就是它），以及展示与归档：

```python
def advise(root: Path, design, runs: list[str], metrics: list[str]) -> dict:
    """问一次 AI 的定性判断。任何问题都抛 LLMUnavailable。"""
    ref = resolve_role(load_config(root), "reason")
    advice = complete(
        ref,
        ADVISE_SYSTEM,
        build_advice_prompt(design.hypothesis, design.dimensions, runs, metrics),
        ADVICE_SCHEMA,
    )
    return check_advice(advice, runs)
```

`plan` 命令在写完预测事件、打印完提示之后追加：

```python
    try:
        advice = advise(root, design, runs, list(design.metrics))
    except LLMUnavailable as exc:
        typer.echo(f"（没有 AI 的那份判断：{exc}）")
        return

    append_event(
        runs_path,
        Event(ts=_now(), type="note", batch=batch_id,
              payload={"kind": "ai_advice", "advice": advice}),
    )
    _show_advice(advice)
```

`_show_advice` 用 rich 打三段，并在最后提示「分歧本身是有信息量的信号」（spec §4.2）。

**存档形式的说明：** 用 `note` 事件而不是新造一个 `ai_advice` 事件类型。spec §3.1 的类型表是 v1 的既定集合，`note` 明确是「自由笔记」，`payload.kind` 足够区分；新增类型要动 `_KNOWN_TYPES` 与 schema 版本语义，而这份材料只是原材料、不参与任何判定，不值得。

- [x] **Step 4: 跑全量测试**
- [x] **Step 5: 提交** — `git commit -m "feat: ari plan 在锁定预测后并排展示 AI 的判断"`

---

## Task 6: `recall.py` + `probing.py` — 有针对性的追问

**Files:**
- Create: `src/ari/recall.py`、`src/ari/probing.py`、`tests/test_recall.py`、`tests/test_probing.py`

spec §7 第 2 步：追问**必须有针对性**——检索本项目历史上相似的 SURPRISE 与 reflection，提出具体假设（「上次调低 lr 你也预期会涨、结果没涨，是否同一原因？」）。检索是纯函数、不碰 LLM，单独可测。

- [x] **Step 1: 写失败的测试**

`tests/test_recall.py`：相似度按「共享的变量维度名 + 共享的指标名 + 偏差方向一致」打分；返回按分数降序的前 N 条，每条带 run key、当初的 rationale、写下的 cause。测试要点：

- 同一维度、同方向偏差的历史 SURPRISE 排在最前；
- 没有历史时返回空列表（不报错）；
- 只返回**已经写过 reflection 的**——没有 cause 的历史提不出有信息量的追问；
- 排除当前这个 run 自己；
- 结果条数有上限（`limit`，默认 3），且超限时的取舍是确定性的（分数相同按批次顺序）。

`tests/test_probing.py`：`PROBE_SCHEMA` 只有 `questions: [str]` 与 `hypotheses: [str]`；`build_probe_prompt` 里必须出现当前 run 的偏差数字、当初的 rationale、以及检索到的历史条目；`check_probe` 拒绝空的 questions。

- [x] **Step 2: 跑测试确认失败**
- [x] **Step 3: 实现两个模块**
- [x] **Step 4: 跑测试确认通过**
- [x] **Step 5: 提交** — `git commit -m "feat: 历史检索与有针对性的复盘追问"`

---

## Task 7: `ari review` 接线与离线降级

**Files:**
- Modify: `src/ari/cli.py`、`src/ari/reviewing.py`、`tests/test_cli_review.py`

追问以**注释**的形式插进复盘草稿的顶部——和预测、实测、偏差摆在一起，人在同一屏里就能回应。不做交互式问答：那会把 review 变成一场对话，而 review 的产物应当是写下来的东西。

- [x] **Step 1: 写失败的测试**
  - 有 LLM 时，草稿里出现追问的文字，且仍然是**一次**编辑器往返；
  - `LLMUnavailable` 时草稿退化成今天的样子，`review` 退出码为 0，reflection 照常写入（spec §8 的「降级为无追问的纯手写模式」）；
  - 追问同样以 `note` 事件归档，`kind == "ai_probe"`，带 batch 与 run；
  - 追问文字进的是注释区，不会被 `parse_reflection` 当成字段读进去。

- [x] **Step 2–4:** `build_reflection_draft(run, ledger=None, probe=None)` 增加一个可选参数，把追问渲染成 `#` 注释块插在偏差信息之后、`cause` 之前。cli 侧包一层 try/except。

- [x] **Step 5: 提交** — `git commit -m "feat: ari review 的针对性追问，不可用时无缝降级"`

---

## Task 8: 端到端、配置模板与文档

**Files:**
- Create: `tests/test_e2e_llm.py`
- Modify: `src/ari/cli.py`（`CONFIG_TEMPLATE`）、`README.md`

- [x] **Step 1: 写端到端测试** `tests/test_e2e_llm.py`

最重要的一条：**完全没有 config.toml 时跑通整条闭环**（`init` 后把 config.toml 删掉），断言 `plan → result → review → board` 每一步退出码都是 0、事件流与不装 LLM 时逐字节一致。这是 spec §8 的验收。

第二条：配好但 provider 报错（monkeypatch `request` 抛 `APIConnectionError`）时同样全绿。

- [x] **Step 2: 更新 `CONFIG_TEMPLATE`** —— 把 `<strong-model>` 换成真实默认值，并写清楚适配器的前提：

```toml
[roles]
# 复盘追问与 plan 阶段的定性判断。
# Anthropic 侧使用 adaptive thinking，需要 Claude 4.6 及以上的模型。
reason = "anthropic:claude-opus-5"
# 日志抽取，v1 未启用（见 spec §5.3）
# extract = "openai:gpt-5.5"
```

注意 `tests/test_cli_init.py` 可能断言了模板内容，一并更新。

- [x] **Step 3: README** —— 新增「AI 那一层」小节：怎么配、三项输出为什么不含数值、以及**不配也能用**这条保证。

- [x] **Step 4: 跑全量测试 + `./bin/ari --help`**

- [x] **Step 5: 提交**

---

## 完成检查

- [x] `uv run pytest` 全绿
- [x] **删掉 config.toml 后整条闭环行为不变**（有测试覆盖）——spec §8
- [x] AI 的输出 schema 里没有任何数值字段（有测试覆盖）——spec §4.2
- [x] LLM 调用发起时预测已经落盘（有测试覆盖）——锚定效应
- [x] 测试全程不打真实 API，不需要任何环境变量
- [x] AI 的输出只进 `note` 归档，不参与任何 verdict
- [ ] **收尾项（需要真实 key，留给人工）：** 用一次真实调用的输出覆盖 `tests/fixtures/llm/*.json`。当前两份 fixture 是照官方文档的响应形状手写的，不是录制的——spec §9 要的是录制，这一条在拿到 key 之前无法完成，不要假装已完成。

## 执行记录

8 个任务全部完成（上面唯一未勾的那条需要真实 API key，做不了）。全量测试 320 passed（计划三结束时 251 + 新增 69）。

与计划的偏差：

1. **依赖版本下限写错过一次。** 初稿凭印象写了 `anthropic>=0.60` / `openai>=1.60`，实际当前版本是 1.0.0 与 3.3.1——那两个下限会让 `uv sync` 装出一个没有 `output_config` 的组合。查 PyPI 后改成 `>=1.0` / `>=3.3`，并在装好后直接对着已安装的 SDK 用 `inspect.signature` 核了一遍参数名与异常类名。
2. **计划外多做了一件事：`tests/conftest.py` 加了一个 autouse fixture，把 provider 的 API key 环境变量从所有测试里摘掉。** 配置模板从 `<strong-model>` 改成真实模型名之后，占位符那道防线就没了；开发机上只要恰好导出过 `ANTHROPIC_API_KEY`，`ari plan` 的测试就会真的发一次请求——花钱、变慢、结果不确定，而且没有任何一条断言会因此变红，根本不会被发现。
3. **`project.py` 的 `RunState` 多了一个 `reflection` 字段。** 计划里没写。`recall.py` 需要 `cause` 的原文做检索，只留一个 `closed` 布尔值就得再遍历一遍事件流。
4. **Task 8 的测试没有先红**，与计划三的 Task 7 同因：前面的任务已经把行为建齐，端到端测试是集成层面的回归网。已手动跑过 `bin/ari` 与草稿渲染核对真实产物。

